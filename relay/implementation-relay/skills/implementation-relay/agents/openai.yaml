interface:
  display_name: "Implementation Relay"
  short_description: "按需委派行为确定的实施切片和风险审核，保留父会话关键判断与最终验收"
  default_prompt: "Use $implementation-relay to handle this project task with the smallest useful set of specialized subagents while the parent agent retains decisions and integration."

policy:
  allow_implicit_invocation: true
